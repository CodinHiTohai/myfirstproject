const CONFIG = {
    // If testing on same WiFi, replace this with your computer's IP address (e.g., http://192.168.1.5:3000)
    // Once hosted live, replace with your live URL (e.g., https://eyein.onrender.com)
    API_URL: window.location.origin, 
    SOCKET_URL: window.location.origin
};
